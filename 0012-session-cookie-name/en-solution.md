Simply set a custom session name:

```yml
session:
    name: MyAppSession
```

Session cookies will no more expose you are using PHP / Symfony, and you can
hide any secret message in it like `WeAreHiringPeopleWatchingAtCookies`.
